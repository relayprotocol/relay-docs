export const SdkRecommendWarning = ({sdkLink}) => (
  <Warning>
It is recommended to use the SDK over the raw APIs. The SDK will iterate the steps and return useful callback information. The APIs will require iterating through the <a href="/references/api/api_core_concepts/step-execution">steps</a> manually.<br/>
{sdkLink ? <b style={{marginTop: "10px", display: "block"}}>Get started with the Relay SDK <a href="/references/relay-kit/sdk/installation">here</a>.</b> : null}
</Warning>)