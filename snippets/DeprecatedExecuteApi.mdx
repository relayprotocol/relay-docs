export const DeprecatedExecuteApi = () => (
  <Warning>
    This API has been replaced by the{" "}
    <a href="/references/api/get-quote-v2">Get Quote API</a>, which supports
    bridging, swapping and calling through a single unified API.
  </Warning>
);
